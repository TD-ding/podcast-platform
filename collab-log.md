# 协作开发日志 — PodWave 播客平台

## 项目概述

类似微博的播客发布平台，支持用户注册登录、发布播客音频、动态流互动（点赞/评论/收藏）、管理员后台管理。

**技术栈**: Node.js Express + HTML/CSS/JS + SQLite + JWT

**仓库**: https://github.com/TD-ding/podcast-platform

---

## 迭代记录

### 第1轮: feat - 初始版本 + 审查修复

**PR**: #1 (`agent/round1` → `master`)

**生成会话任务**:
- 搭建项目结构（backend/ + frontend/）
- 实现用户注册/登录（JWT 认证，bcrypt 加密）
- 实现发布播客（multer 音频上传）
- 实现动态流展示、点赞、评论
- 实现管理员后台（仪表盘、用户管理、内容审核）
- 配置 ESLint v9+ flat config

**审查会话反馈（27项）**:
- 安全：JWT 硬编码密钥、默认密码、CORS 无限制
- 性能：列表无分页、缺索引、bcrypt 同步阻塞
- 代码：重复 SQL、默认 status 跳过审核、删除不清理文件
- 前端：XSS 风险、导航重复、Token 不过期

**模糊化输入**:
> "安全方面，那个密码密钥好像直接写在代码里了，万一别人看到代码不安全吧？能不能换个方式处理。然后管理员默认密码也太简单了。还有接口好像什么网站都能调用，能不能限制一下。性能方面，播客列表和评论好像是一次性全部加载的，能不能加个分页。然后数据库那边也没加什么索引。代码方面有几处查询代码几乎一模一样，能不能整理一下。然后新发的播客好像直接就通过了，不是说要有管理员审核吗？还有删除播客的时候音频文件好像还留在服务器上。前端方面文件名如果包含特殊字符好像会出问题，导航栏每个页面都写了一遍好多重复代码。还有登录状态好像不会过期。"

**修复**:
- JWT_SECRET / ADMIN_PASSWORD 改为环境变量，缺少拒绝启动
- CORS 限制为配置的 origin
- 播客列表和评论 API 加分页
- 数据库加 6 个索引
- 新播客默认 pending 状态
- 删除播客时清理磁盘音频文件
- 导航栏抽取为公共函数
- Token 过期自动检测和跳转

**迭代链路**: 生成会话(初始) → 优化会话(审查) → **主会话模糊化** → 生成会话(本PR)

---

### 第2轮: refactor - 代码质量优化

**PR**: #2 (`agent/round2` → `master`)

**审查会话反馈（21项）**:
- 代码结构：SECRET 导出暴露、上传失败文件未清理、plays 返回旧值、同步文件操作、bcrypt 仍同步、admin seed 副作用
- 错误处理：无全局错误中间件、multer 错误未捕获、DB 无事务、静默 catch
- 用户体验：audio MIME 写死、audio_path 未转义、无 loading 状态、401 直接跳转丢状态

**模糊化输入**:
> "程序如果出错了好像没什么兜底处理，会不会直接崩掉？上传文件太大的情况用户也看不到提示。发播客的时候万一中间出错了数据会不会不一致？密码加密还是同步的，密钥从中间件导出给了其他地方用不太安全。上传失败时音频文件还是留在服务器上。管理后台用户列表也是一次全加载。页面加载时一片空白不知道是在加载还是出了问题。登录过期了直接跳走正在做的东西就没了。上传了 wav 格式的文件播不了，是不是只支持 mp3？用户名注册什么字符都能输。"

**修复**:
- Express 全局错误中间件 + Multer 错误边界
- bcrypt 改异步，SECRET 不导出改用 signToken()
- 发布播客用 db.transaction 原子操作
- 上传失败自动清理磁盘文件
- 管理后台用户列表加分页
- 前端 loading spinner
- token 过期先弹 alert 再跳转，redirect 回原页面
- 音频 MIME type 自动识别
- 用户名注册字符集限制

**迭代链路**: 生成会话(初始) → 优化会话(审查) → **主会话模糊化** → 生成会话(本PR)

---

### 第3轮: feat - 用户体验优化

**PR**: #3 (`agent/round3` → `master`)

**审查会话反馈（14项）**:
- 功能缺失：个人资料、密码修改、搜索、用户主页、播客详情页、评论分页、点赞状态、删除评论
- 体验优化：音频懒加载、管理试听、上传进度条、移动端导航

**模糊化输入**:
> "点赞之后刷新页面赞就变回空心了好像功能坏了。播客多了想找某个找不到能不能加搜索框。点击作者名字什么反应都没有能不能跳到作者主页。注册之后连改头像和简介的地方都没有。密码也改不了。播客详情都挤在列表卡片里太乱能不能做成单独页面。评论超过50条后面看不到能不能加加载更多。自己发的评论删不了。首页同时加载所有音频好慢。上传大文件没有进度条。管理员审核只能看标题听不了。手机上导航栏挤在一起了。"

**修复**:
- 点赞状态持久化（批量查询）
- 搜索功能（按标题关键词）
- 用户主页（点击作者跳转）
- 个人设置页（修改简介、修改密码）
- 播客详情页（大播放器 + 完整评论 + 加载更多 + 删除评论）
- 音频 preload="none" 懒加载
- 上传进度条（XHR upload progress）
- 管理后台试听按钮
- 移动端汉堡菜单

**迭代链路**: ...→ 优化会话(审查) → **主会话模糊化** → 生成会话(本PR)

---

### 第4轮: feat - 功能增强

**PR**: #4 (`agent/round4` → `master`)

**审查会话反馈（14项高级特性建议）**:
- 社交互动：关注/订阅、收藏/稍后听、分享、评论回复、@提及
- 通知体系：站内通知、通知铃铛、邮件通知、审核状态推送
- 平台运营：排行榜、标签分类、封面图、播放历史、数据统计

**模糊化输入**:
> "想加个收藏功能保存播客以后再听。想分享播客给朋友一键复制链接。发布播客时能选封面图。播客审核通过或被拒绝了完全不知道能不能有通知？评论和点赞也通知一下，导航栏显示铃铛和未读数。加个热门排行榜看看哪些最受欢迎。"

**修复**:
- 收藏/稍后听（favorites 表 + 我的收藏标签页）
- 一键分享复制链接
- 封面图上传（JPG/PNG/GIF/WebP，最大10MB）
- 站内通知系统（notifications 表 + 铃铛未读数 + 通知列表页）
- 审核结果/评论/点赞自动触发通知
- 热门排行榜（按播放量+点赞数加权排序）

**迭代链路**: ...→ 优化会话(审查) → **主会话模糊化** → 生成会话(本PR)

---

### 第5轮: fix - Bug修复

**PR**: #5 (`agent/round5` → `master`)

**审查会话反馈（12项）**:
- 实际Bug：keyword 查询别名不一致、通知链接 XSS、首页/热门页 401 不跳转
- 前后端不一致：用户主页缺封面/收藏按钮、详情页封面样式不同、我的播客点赞显示不对、封面图上传被拦截
- 边界情况：播放计数含爬虫、通知内容无截断、JWT 解析容错、封禁用户旧 token、管理员删不了评论

**模糊化输入**:
> "封面图上传选了图片就报错是不是把图片当音频检查了。用户主页没有封面图和收藏分享按钮。详情页封面显示和其他页面不一样。我的播客页面点赞显示不对。首页和热门页登录过期了不跳转只显示错误。通知链接有特殊字符会出问题。搜索有时候查数据出问题。播放次数被爬虫刷了。管理员删不了评论。登录状态判断有时候出错。"

**修复**:
- 封面图 fileFilter 按 fieldname 区分音频/图片
- 用户主页/详情页/我的播客样式统一
- 首页和热门页改用 api() 统一 401 跳转
- 通知链接 escapeHtml
- 搜索 LIKE 通配符转义
- 播放计数排除 bot/crawler
- 管理员可删除任意评论
- JWT token 解析容错

**迭代链路**: ...→ 优化会话(审查) → **主会话模糊化** → 生成会话(本PR)

---

### 额外: feat - 测试 + Docker + CI

**PR**: #6 (`agent/lint-test-docker-ci` → `master`)

**内容**:
- 47 个单元测试（auth 15 + podcasts 21 + admin 11）
- Dockerfile multi-stage build
- docker-compose.yml
- .github/workflows/ci.yml + cd.yml
- .env.example + .gitignore 更新
- API 路由注释

### 额外: docs - 项目文档

**PR**: #7 (`agent/docs` → `master`)

**内容**:
- docs/frontend.md — 前端架构文档
- docs/backend.md — 后端 API 文档
- docs/admin-frontend.md — 管理员面板文档
- docs/deployment.md — 部署文档
